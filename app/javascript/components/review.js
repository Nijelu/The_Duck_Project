document.getElementById('review-form').addEventListener('submit', (event) => {
  event.preventDefault();
  const message = document.getElementById('message-object').value;
  const name = document.getElementById('message-content').value;
});
